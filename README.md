# iterative_code_to-the-recursive-code
*Convert the following iterative code to the recursive code. Determine which code should be in recursive function (base case, recursive case) and what should be in main
